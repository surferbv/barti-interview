import { TextField } from "@mui/material";

function SearchBar() {
  return (
    <TextField
      id="standard-basic"
      label="Search"
      variant="standard"
      width="40%"
    ></TextField>
  );
}

export default SearchBar;
